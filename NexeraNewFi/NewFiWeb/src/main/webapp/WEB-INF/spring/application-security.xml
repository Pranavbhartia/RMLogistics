<beans:beans xmlns="http://www.springframework.org/schema/security"

	xmlns:beans="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">
 
 	
 
	<http auto-config="true" use-expressions="true">
	<!-- 	<intercept-url pattern="/customerFileUpload.do" access="permitAll" />
		<intercept-url pattern="/newfi/**" access="permitAll" />
        <intercept-url pattern="/login.do" access="permitAll" />
        <intercept-url pattern="/logout.do" access="permitAll" />
        <intercept-url pattern="/accessdenied.do" access="permitAll" />
        <intercept-url pattern="/**" access="hasRole('ROLE_USER')" /> -->
        <form-login login-page="/login.do" default-target-url="/list.do" authentication-failure-url="/accessdenied.do" username-parameter="username"
			password-parameter="password"/>
        <logout logout-success-url="/logout.do" />
        
        
   </http>
 
    <authentication-manager alias="authenticationManager">
        <authentication-provider user-service-ref="myUserDetailsService" >
        </authentication-provider>
    </authentication-manager>
    
    <beans:bean id='myUserDetailsService' class='com.nexera.newfi.web.authentication.AuthenticateUser'/>
	
	
 
</beans:beans>